pr:
  branches:
    include:
    - main
  paths:
    include:
    - pipelines/code
    exclude:
    - pipelines/test01
    - pipelines/test02

steps:
- script: echo This pipeline will be triggered by Pull Request !